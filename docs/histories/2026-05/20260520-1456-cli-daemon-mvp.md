## [2026-05-20 14:56] | Task: CLI daemon MVP

### 🤖 Execution Context

- **Agent ID**: `Codex`
- **Base Model**: `GPT-5`
- **Runtime**: `Codex CLI`

### 📥 User Query

> 设计并推进一个轻量 CodexClaw CLI daemon：用 Go/Cobra 定时执行 Codex，让 Codex 检查是否有任务需要执行；追求简单高效，参考本地 Codex 和 OpenClaw 仓库。

### 🛠 Changes Overview

**Scope:** `cmd/codexclaw`, `internal/cli`, `internal/daemon`, `docs`

**Key Actions:**

- **CLI MVP**: 新增 `codexclaw daemon once` 和 `codexclaw daemon run`。
- **Codex Runner**: 通过 `codex exec --cd <workspace>` 执行 heartbeat prompt，并支持 sandbox/model/profile/额外参数透传。
- **Daemon Loop**: 增加串行 interval tick、per-run timeout 和本地 lock。
- **Docs Sync**: 更新架构、设计、稳定性、安全和质量文档。
- **CI Sync**: `scripts/ci.sh` 接入 `go test ./...`，GitHub Actions 显式 setup Go。

### 🧠 Design Intent (Why)

先把 CodexClaw 定位成“唤醒 Codex 的薄调度器”，避免第一版复制 OpenClaw Gateway 或直接承担 Codex App Server 的 thread/event 协议复杂度。CLI runner 足够验证最小路径，后续再按痛点抽象 App Server runner。

### 📁 Files Modified

- `cmd/codexclaw/main.go`
- `internal/cli/root.go`
- `internal/cli/daemon.go`
- `internal/daemon/runner.go`
- `internal/daemon/loop.go`
- `internal/daemon/lock.go`
- `internal/daemon/runner_test.go`
- `docs/design-docs/cli-daemon.md`
- `docs/design-docs/index.md`
- `docs/ARCHITECTURE.md`
- `docs/RELIABILITY.md`
- `docs/SECURITY.md`
- `docs/QUALITY_SCORE.md`
- `docs/CICD.md`
- `README.md`
- `.github/workflows/ci.yml`
- `scripts/ci.sh`

### 🔁 Follow-up: Config-driven single task

用户进一步收敛第一版设计：不需要 heartbeat 机制，只需要像 shell loop 一样定时执行 `codex exec`。repo 目录、执行间隔和 prompt 放到本机全局 `~/.codexclaw/config.toml` 中，第一版只针对一个任务。

本次跟进调整：

- 移除 `HEARTBEAT_OK` 语义和 heartbeat ack 分类。
- 新增 `~/.codexclaw/config.toml` 加载，配置 `workspace`、`interval`、`timeout`、`prompt` 和 Codex 透传项。
- CLI 收敛为 `daemon once/run --config --dry-run`。
- 默认本机配置指向用户指定的 aifi 仓库。
- 同步 README、架构、设计、稳定性、安全和质量文档。

### 🔁 Follow-up: Config instructions command

用户希望 CodexClaw 提供一个子命令，运行后直接返回文本内容，用来指导 AI 如何增加和修改全局配置文件。

本次跟进调整：

- 新增 `codexclaw config instructions`。
- 命令只向 stdout 打印 `~/.codexclaw/config.toml` 的字段、示例和编辑规则。
- README 和 CLI daemon 设计文档补充该命令。
