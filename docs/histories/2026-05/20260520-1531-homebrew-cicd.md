## [2026-05-20 15:31] | Task: Homebrew CI/CD cleanup

### 🤖 Execution Context

- **Agent ID**: `Codex`
- **Base Model**: `GPT-5`
- **Runtime**: `Codex CLI`

### 📥 User Query

> 提交推送。然后清理无用 CI/CD，增加一个可以发送到 Homebrew 的 CI/CD。Homebrew publishing trust 后续用 OBU 配置。

### 🛠 Changes Overview

**Scope:** `.github/workflows`, `scripts`, `docs`

**Key Actions:**

- 提交前一阶段 CodexClaw daemon MVP。
- 删除模板化 release workflow、供应链扫描 workflow、dependency review 配置和占位 release 打包脚本。
- 新增 `homebrew.yml`，在 `v*` tag 或手动触发时生成 Homebrew formula 并推送到 tap。
- 新增 `scripts/generate-homebrew-formula.sh`，把 formula 生成逻辑沉淀为本地可验证脚本。
- 更新仓库卫生检查，使必需 workflow 从通用 release/supply-chain 切换为 Homebrew 发布。
- 同步 `docs/CICD.md` 和 `docs/SUPPLY_CHAIN_SECURITY.md`。

### 🧠 Design Intent (Why)

第一版保持 less is more：保留基础 CI，发布链路只做 Homebrew tap 更新，不再维护模板期的 SBOM、provenance 和通用 release artifact。Homebrew formula 引用 GitHub tag 源码包，在安装时用 Go 构建，避免额外二进制制品和 GoReleaser 配置。

### 📁 Files Modified

- `.github/workflows/homebrew.yml`
- `.github/workflows/release.yml`
- `.github/workflows/supply-chain-security.yml`
- `.github/dependency-review-config.yml`
- `scripts/generate-homebrew-formula.sh`
- `scripts/check-repo-hygiene.sh`
- `scripts/release-package.sh`
- `docs/CICD.md`
- `docs/SUPPLY_CHAIN_SECURITY.md`
